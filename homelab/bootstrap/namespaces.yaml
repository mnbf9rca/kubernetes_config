apiVersion: v1
kind: Namespace
metadata:
  name: downloads
---
# NOTE: cert-manager, traefik, keel, and local-path-storage namespaces
# are created by their respective upstream manifests (see homelab/bootstrap/*/).
# Don't list them here — kustomize rejects duplicate resources.
apiVersion: v1
kind: Namespace
metadata:
  name: backup
