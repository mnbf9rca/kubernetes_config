{
  "name": "influxdb-mcp-image",
  "version": "0.0.0",
  "private": true,
  "description": "Build inputs for ghcr.io/mnbf9rca/influxdb-mcp-server: the upstream server plus one extra tool, added by a --import hook rather than a fork.",
  "dependencies": {
    "influxdb-mcp-server": "0.2.0"
  }
}
