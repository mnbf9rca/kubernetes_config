apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Secrets with ${VAR} placeholders that envsubst fills from direnv-loaded env vars.
# Nothing in this directory should contain a plaintext secret value.

resources:
  - route53.yaml
