---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-config
  namespace: downloads
spec:
  accessModes: [ReadWriteOnce]
  resources: { requests: { storage: 20Gi } }
  storageClassName: local-path
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: radarr-media-nfs-pv
spec:
  capacity: { storage: 10Ti }
  accessModes: [ReadWriteMany]
  storageClassName: ""
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: nfs.csi.k8s.io
    volumeHandle: radarr-media-nfs
    volumeAttributes:
      server: "10.10.10.1"
      share: "/tank/video"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-media
  namespace: downloads
spec:
  accessModes: [ReadWriteMany]
  storageClassName: ""
  resources: { requests: { storage: 10Ti } }
  volumeName: radarr-media-nfs-pv
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: radarr
  namespace: downloads
  annotations:
    keel.sh/policy: force
    keel.sh/match-tag: "true"
    keel.sh/trigger: poll
    keel.sh/pollSchedule: "@every 6h"
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector: { matchLabels: { app: radarr } }
  template:
    metadata:
      labels: { app: radarr }
    spec:
      dnsPolicy: None
      dnsConfig:
        nameservers: ["8.8.8.8", "8.8.4.4"]
      containers:
        - name: radarr
          image: linuxserver/radarr:latest
          imagePullPolicy: Always
          env:
            - name: PUID
              value: "1999"
            - name: PGID
              value: "1999"
            - name: TZ
              value: Europe/London
          ports:
            - containerPort: 7878
              name: http
          startupProbe:
            httpGet: { path: /, port: http }
            failureThreshold: 15
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /, port: http }
            failureThreshold: 5
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /config
            - name: media
              mountPath: /media
        - name: sqlite-snapshot
          image: alpine:3.20
          securityContext:
            runAsUser: 1999
            runAsGroup: 1999
          command: ["sh", "-c"]
          args:
            - |
              apk add --no-cache sqlite
              while true; do
                if [ -f /config/radarr.db ]; then
                  sqlite3 /config/radarr.db ".backup /config/radarr.db.restic" 2>&1 || true
                fi
                sleep 43200
              done
          volumeMounts:
            - name: config
              mountPath: /config
      volumes:
        - name: config
          persistentVolumeClaim: { claimName: radarr-config }
        - name: media
          persistentVolumeClaim: { claimName: radarr-media }
---
apiVersion: v1
kind: Service
metadata:
  name: radarr
  namespace: downloads
spec:
  selector: { app: radarr }
  ports:
    - port: 7878
      targetPort: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: radarr
  namespace: downloads
spec:
  ingressClassName: traefik
  rules:
    - host: radarr.cynexia.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: radarr
                port:
                  number: 7878
