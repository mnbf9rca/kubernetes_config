apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Top-level homelab kustomization. Apply order within a single build is
# determined by kustomize, but at bring-up time apply bootstrap first and
# verify each component is healthy before adding workloads.

resources:
  - bootstrap
  - secrets
  - workloads
  - backup
