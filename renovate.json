{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "enabledManagers": ["kubernetes", "kustomize"],
  "recreateWhen": "always",
  "pinDigests": true,
  "minimumReleaseAge": "3 days",
  "kubernetes": {
    "managerFilePatterns": [
      "/^homelab/.+\\.ya?ml$/",
      "/^vps/.+\\.ya?ml$/"
    ]
  },
  "kustomize": {
    "managerFilePatterns": [
      "/(^|/)kustomization\\.ya?ml$/"
    ]
  },
  "description": [
    "SETTING ignorePaths REPLACES THE config:recommended DEFAULTS. config:recommended pulls in :ignoreModulesAndTests, whose ignorePaths cover node_modules, bower_components, vendor, and assorted test/fixture directories. Declaring this key overrides that list wholesale rather than adding to it. No practical effect here - this repo manages no YAML under any of those paths - but a future manager (npm, gomod) added to enabledManagers would scan vendored trees this list no longer excludes. Re-add the defaults if that happens.",
    "legacy-microk8s/** and no_longer_used/** currently match NOTHING. Both managerFilePatterns are anchored to ^homelab/ and ^vps/, so neither frozen tree is scanned in the first place. They are listed as defence in depth against a future pattern that is not anchored, not as active exclusions. **/secrets/** is the opposite and is the one genuinely active entry of the three: the homelab pattern does match homelab/secrets/*.yaml, and this excludes it. Harmless either way, since those files hold only ${VAR} placeholders and name no image, but the exclusion is real rather than defensive."
  ],
  "ignorePaths": [
    "**/secrets/**",
    "legacy-microk8s/**",
    "no_longer_used/**"
  ],
  "packageRules": [
    { "matchFileNames": [
        "homelab/workloads/**",
        "homelab/bootstrap/keel/**",
        "vps/workloads/**",
        "vps/bootstrap/keel/**",
        "vps/bootstrap/cloudflared/**"
      ],
      "pinDigests": false,
      "description": "NO DIGEST PINNING ON THE KEEL-MANAGED TREES. pinDigest is an updateType that fires on any Docker dependency without a digest, floating tags INCLUDED, so top-level pinDigests plus the widened scope would have Renovate open 'Pin Docker digests' pull requests against linuxserver/emby:latest, cloudflare/cloudflared:latest, karakeep:release and the rest — the images keel is supposed to own. Merging one produces the pinned-tag-with-keel-annotations state this whole strategy exists to abolish, and creates permanent keel-versus-git drift: keel rewrites the live Deployment's digest every six hours while the repo holds the merged one, so make diff-homelab reports a changed Deployment forever and the read-the-diff-in-full discipline dies. The keel Deployments are listed because they are pinned by digest ALREADY, by hand, and Renovate re-proposing that pin adds nothing." },
    { "matchFileNames": ["homelab/health/**"],
      "groupName": "health stack",
      "automerge": false,
      "prPriority": 5 },
    { "matchFileNames": ["homelab/ops/**"],
      "groupName": "ops tooling",
      "automerge": false,
      "prPriority": 1,
      "description": "The update watcher's own image — watched so the watcher is not the one unwatched pin in the estate." },
    { "matchFileNames": [
        "homelab/ops/keel-fresh.yaml",
        "vps/ops/keel-fresh.yaml"
      ],
      "groupName": "keel-fresh copy pair",
      "description": "ONE PULL REQUEST, BOTH COPIES. These two files are an ENFORCED COPY PAIR: `make check-keel-fresh-parity` requires them byte-identical outside a short list of sanctioned differences, and the image line is NOT on that list. So a bump taken on one cluster alone fails the guard, and the guard has no per-cluster half — a divergence refuses apply-homelab AND apply-vps until the other copy lands. Renovate split the curlimages/curl bump into two pull requests (70 for homelab, 74 for the VPS), each individually invalid. The shared groupName makes the pair arrive as a single pull request. This rule sits AFTER the homelab/ops/** rule on purpose: later rules win per field, so this groupName overrides 'ops tooling' for the homelab copy, while that rule's automerge and prPriority still apply. Any future enforced copy pair needs the same treatment." },
    { "matchFileNames": ["homelab/hindsight/**"],
      "groupName": "hindsight stack",
      "automerge": false,
      "prPriority": 5,
      "description": "Pinned, no keel. Upgrades run the dump-first runbook: make hindsight-upgrade, then merge, diff, apply." },
    { "matchUpdateTypes": ["major"],
      "dependencyDashboardApproval": true,
      "description": "Every major waits on the dependency dashboard. The update session reviews the dashboard and approves what it wants to take; an unattended major PR against a stateful workload is exactly the ceremony this estate wants to keep manual." },
    { "matchUpdateTypes": ["major"],
      "matchPackageNames": ["postgres", "pgvector/pgvector"],
      "enabled": false,
      "description": "PostgreSQL major bumps are a dump-and-restore, not a tag edit. BOTH names are listed: `pgvector/pgvector` is hindsight's database and `postgres` is umami's database and hindsight's dump client, and naming only the first is how PR #66 slipped through. Order against the all-majors rule above is NOT load-bearing: Renovate merges matching rules later-wins PER FIELD, and `enabled` and `dependencyDashboardApproval` are different fields, so `enabled: false` applies from either position. Do not reorder these two believing it changes the outcome." },
    { "matchPackageNames": ["kroniak/ssh-client"],
      "dependencyDashboardApproval": true,
      "description": "HOLD ON THE DASHBOARD, DO NOT AUTO-PROPOSE. homelab/backup/hermes-pull.yaml pins this by DIGEST WITH NO TAG, so Renovate resolves it against `latest` and every repoint of that tag becomes a digest-update proposal. A digest update is not a major, so the all-majors dashboard rule above does not catch it, and the 2026-08-26 scope widening to homelab/** is what started these arriving at all. This container handles an SSH private key and a zip full of live secrets, and the image is an unofficial personal Docker Hub repository - the exact case where an unattended bump is a security event rather than a convenience. When approving one, treat it as a NEW IMAGE: re-run the in-cluster probe from the design rollout section (ssh, sha256sum, wget, awk, du, od, head, stat present; the hermes-passwd mount is what makes `ssh -V` work as uid 1999) before applying. The same warning is in the manifest, twenty lines above the image line where a pull request diff will not show it; this rule is how it reaches the reviewer." },
    { "matchPackageNames": ["thisisarpanghosh/garmin-fetch-data"],
      "dependencyDashboardApproval": true,
      "description": "Digest-pinned to a main build; the v0.5.0 release is broken (client.profile crash). Dashboard-only rather than disabled, so an upstream fix surfaces without auto-PRing a regression." }
  ]
}
