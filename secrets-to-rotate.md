# Secrets to rotate (honesty box)

When a real secret **value** is disclosed anywhere it doesn't belong — echoed to a
terminal, printed in an agent report or transcript, pasted into a chat, written to
a log or scratch file — the discloser records it here immediately, even when
unsure whether the exposure "counts". Entries are identifiers only: **never write
the secret value itself in this file.**

An entry here means: assume compromised, rotate at the next opportunity, then mark
it rotated. The rule that puts entries here is in `AGENTS.md` ("Secret disclosure →
honesty box"); the rotation *procedures* are in the docs tree — the general one in
[docs/operations/apply-workflow.md](docs/operations/apply-workflow.md#rotating-a-secret),
and the `health-*` items (including InfluxDB tokens, which must be re-minted rather than
edited) in
[docs/operations/homelab-health.md](docs/operations/homelab-health.md#secret-rotation).

| Date | Secret (op:// reference or k8s secret/key) | How it was disclosed | Disclosed by | Status |
|------|--------------------------------------------|----------------------|--------------|--------|
| 2026-07-26 | op://Homelab/health-influxdb/admin-password | `op item get --format json` printed plaintext into local subagent transcript | Task 9 implementer agent | rotated 2026-07-26 |
| 2026-07-26 | op://Homelab/health-influxdb/admin-token | same event as above | Task 9 implementer agent | rotated 2026-07-26 |
| 2026-07-26 | op://Homelab/health-influxdb/garmin-v1-password | same event as above | Task 9 implementer agent | rotated 2026-07-26 |
| 2026-07-26 | op://Homelab/health-influxdb/admin-password | `op item get health-influxdb --fields label=admin-password --format json` run mid-rotation printed a partial (~29 char) plaintext fragment of the then-current admin-password value into the rotation agent's tool output/transcript, while checking whether a prior `op item edit` had landed | Rotation agent (this task) | rotated 2026-07-26 (superseded immediately by a freshly generated replacement password before this exposure was even logged) |
| 2026-07-26 | op://Homelab/health-healthchecks/backup-uuid (HEALTH_HC_BACKUP_UUID) | `make build-homelab` rendered output was grepped/printed to verify envsubst substitution while implementing Task 12; the resolved ping UUID appeared in the agent's tool output/transcript | Task 12 implementer agent | accepted, no rotation (operator decision 2026-07-27): ping UUIDs are spam-target identifiers, not secrets — they grant no access. The value appeared only in a local session transcript and has never been committed to this repo; keeping it out of GitHub (op:// reference only) is the actual control, and that holds. |
| 2026-07-26 | op://Homelab/health-influxdb/ingester-token | `op item get health-influxdb --fields label=ingester-token --reveal | head -c 200` printed the full field value into the agent's tool output/transcript during Attempt 3 of the token-recovery task (step 2 verification) — should have used command substitution into a shell variable only | Attempt 3 recovery agent | rotated 2026-07-26 — new InfluxDB auth minted (same write-only scope), 1Password field overwritten, old auth deleted server-side, new value verified live (200) |
| 2026-08-20 | op://Homelab/health-healthchecks/apple-uuid + garmin-uuid (HEALTH_HC_APPLE_UUID, HEALTH_HC_GARMIN_UUID) | While building a throwaway test pod to verify the reworked `ingest-freshness` query, a `kubectl get cronjob -o json \| jq` pipeline wrote the live CronJob's env block — including both resolved ping UUIDs — to a local scratch file (`ov3.json`) in the session scratchpad. Never printed to terminal or transcript (all displayed pod output was UUID-redacted); the file was rebuilt without real UUIDs within the same task and deleted at cleanup. | Task 2 (health lane) implementer agent | accepted, no rotation (operator decision 2026-08-20): ping UUIDs are spam-target identifiers, not secrets — they grant no access. Same reasoning as HEALTH_HC_BACKUP_UUID on 2026-07-27. The control that matters is keeping them out of the public repo (op:// reference only), which holds. |
| 2026-08-20 | FreshRSS PubSubHubbub per-feed callback secrets (`k=` query parameter; FreshRSS-generated, not in 1Password) | Surfaced in a read-only traffic-analysis agent's tool output while reading `/var/www/FreshRSS/data/users/_/log_pshb.txt` and the Apache access log inside the `freshrss` pod. Never written to a file or committed. **Re-disclosed 2026-08-20** by the main session, which `cat`-ed all twelve `data/PubSubHubbub/feeds/*/!hub.json` files while diagnosing the WebSub failure, printing every per-feed `key` into the session transcript. | Traffic-analysis agent (log investigation); re-disclosure by main session | pending operator decision — these authenticate inbound WebSub push callbacks per feed. They rotate naturally when a feed re-subscribes, and the 2026-08-20 `base_url` fix forces all twelve to re-subscribe, so no manual rotation is needed. The callbacks were 301-redirected and failing throughout, because they were registered as http:// rather than https://. |
| 2026-08-20 | Luma feed API key (`?key=` parameter of the `visionary-swan.pikapod.net/luma/london-mlops` feed URL; not in 1Password) | Same event — appeared in the agent's tool output while reading `/var/www/FreshRSS/data/users/ruined0346/log.txt`. Never written to a file or committed. **Re-disclosed 2026-08-20** by the main session: `cli/actualize-user.php` emits a per-domain retry warning containing the whole feed URL, key included, and that ran during the WebSub diagnosis. | Traffic-analysis agent (log investigation); re-disclosure by main session | pending operator action — regenerate at source (Luma), then update the subscription URL in FreshRSS. This value reappears in FreshRSS's own logs on every refresh cycle, so it keeps resurfacing until the URL is replaced. |
