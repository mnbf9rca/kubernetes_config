.mcp.json
settings.local.json
docs/superpowers/
.playwright-mcp/

# Agent worktrees (created by Claude Code isolation: worktree)
.claude/worktrees/

# Byte-code from running the health scripts tests locally.
__pycache__/
*.pyc

# A local `npm install` in homelab/health/mcp/ to refresh package-lock.json.
# The lockfile is committed; the tree it describes is not.
node_modules/
