apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# No top-level namespace override. Each manifest declares its own namespace.

resources:
  - bootstrap
  - secrets
  - workloads
  - backup
